# dependencies
node_modules/

# config (contains API keys)
.decipher/

# OS
.DS_Store
Thumbs.db

# editor
*.swp
*.swo
*~
.vscode/
.idea/

# local agent tooling
.claude/
.codex
AGENTS.md
CLAUDE.md

# reference material (read-only, not part of the product)
references/
scenarios/
tests/

# agent contract docs (tracked separately from product code)
decipher-agent-docs/

# design docs and task tracking (local only)
docs/

# Rust build artifacts
target/
tui/target/

# Compiled TUI binary — built via `cargo build --release`, not committed.
# The JS shim (bin/decipher) is source and IS tracked.
bin/decipher-tui

# build artifacts
*.tgz
*.bak

# logs
*.log
!scenarios/*/logs/*.log

# env files
.env
.env.*
!.env.example

# plugin lock files
skills-lock.json

# package manager lock files (not committed for this project)
pnpm-lock.yaml
package-lock.json
yarn.lock

# Homebrew formula (managed in a separate tap repository)
Formula/

# TUI session recordings (DECIPHER_TUI_RECORD_SESSION)
*.session.jsonl
session-*.jsonl

tmp/
