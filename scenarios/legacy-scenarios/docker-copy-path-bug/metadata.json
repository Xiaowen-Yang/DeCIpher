{
  "id": "docker-copy-path-bug",
  "category": "docker",
  "difficulty": "easy",
  "expected_classification": "path_or_copy_error",
  "auto_fixable": true,
  "execution_mode": "docker_run",
  "mission_type": "repair",
  "mission_stop_boundary": "issue_resolved",
  "workspace_mode": "temp_then_writeback",
  "writeback_on_success": true,
  "repair_target_files": ["Dockerfile"],
  "required_tools": ["docker"],
  "verification": ["docker_build", "docker_run"],
  "demo_time_seconds": 25,
  "broken_files": ["Dockerfile"],
  "log_file": "logs/build-failure.log",
  "docker_verified": true,
  "verification_command": "bash -lc 'set -euo pipefail; img=decipher-cpb-check-$$; cleanup(){ docker rmi \"$img\" -f >/dev/null 2>&1 || true; }; trap cleanup EXIT; docker build -t \"$img\" scenarios/docker-copy-path-bug/expected -q >/dev/null; docker run --rm \"$img\" >/tmp/decipher-cpb-run.log; grep -q \"copy-path scenario app\" /tmp/decipher-cpb-run.log && echo PASS || echo FAIL'"
}
