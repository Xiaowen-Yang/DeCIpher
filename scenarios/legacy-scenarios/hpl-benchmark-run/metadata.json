{
  "id": "hpl-benchmark-run",
  "category": "docker",
  "difficulty": "medium",
  "description": "HPL benchmark — build, start, and run the benchmark to completion",
  "execution_mode": "docker_run",
  "workspace_mode": "temp_then_writeback",
  "writeback_on_success": true,
  "mission_type": "benchmark_run",
  "mission_stop_boundary": "benchmark_completed",
  "required_tools": ["docker"],
  "broken_files": ["Dockerfile"],
  "log_file": "logs/build-failure.log",
  "expected_classification": "dependency_install_failure",
  "verification_command": "docker build -t decipher-hpl-bench scenarios/hpl-benchmark-run/expected -q && docker run --rm decipher-hpl-bench echo 'benchmark_complete' | grep -q benchmark_complete && echo PASS || echo FAIL",
  "demo_time_seconds": 60
}
