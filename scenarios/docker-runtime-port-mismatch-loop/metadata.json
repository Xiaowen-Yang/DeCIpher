{
  "id": "docker-runtime-port-mismatch-loop",
  "category": "docker",
  "difficulty": "medium",
  "expected_classification": "healthcheck_startup_failure",
  "auto_fixable": true,
  "execution_mode": "healthcheck",
  "workspace_mode": "temp_then_writeback",
  "writeback_on_success": true,
  "repair_target_files": ["Dockerfile"],
  "required_tools": ["docker"],
  "broken_files": ["Dockerfile"],
  "log_file": "logs/healthcheck-failure.log",
  "verification": ["docker_run", "healthcheck"],
  "demo_time_seconds": 45,
  "docker_verified": true,
  "verification_command": "docker build -t decipher-pml-check scenarios/docker-runtime-port-mismatch-loop/expected -q && grep -q 'localhost:3000' scenarios/docker-runtime-port-mismatch-loop/expected/Dockerfile && docker rmi decipher-pml-check -f >/dev/null 2>&1 && echo PASS || echo FAIL"
}
