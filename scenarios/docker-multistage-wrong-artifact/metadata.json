{
  "id": "docker-multistage-wrong-artifact",
  "category": "docker",
  "difficulty": "medium",
  "expected_classification": "path_or_copy_error",
  "auto_fixable": true,
  "execution_mode": "docker_run",
  "workspace_mode": "temp_then_writeback",
  "writeback_on_success": true,
  "repair_target_files": ["Dockerfile"],
  "required_tools": ["docker"],
  "docker_verified": true,
  "verification": ["docker_build", "docker_run"],
  "demo_time_seconds": 35,
  "broken_files": ["Dockerfile"],
  "log_file": "logs/build-failure.log",
  "verification_command": "bash -lc 'set -euo pipefail; img=decipher-ms-check-$$; cleanup(){ docker rmi \"$img\" -f >/dev/null 2>&1 || true; }; trap cleanup EXIT; docker build -t \"$img\" scenarios/docker-multistage-wrong-artifact/expected -q >/dev/null; docker run --rm \"$img\" >/tmp/decipher-ms-run.log; grep -q \"app running\" /tmp/decipher-ms-run.log && echo PASS || echo FAIL'"
}
