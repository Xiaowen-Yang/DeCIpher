[package]
name = "decipher-policy"
version = "0.1.5"
edition = "2021"
description = "Execution policy engine — tool classification, path protection, approval decisions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
