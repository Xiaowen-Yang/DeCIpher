[package]
name = "decipher-providers"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Provider abstraction for LLM APIs (Anthropic implemented, OpenAI planned)"

[dependencies]
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
futures = { workspace = true }
reqwest = { version = "0.12", features = ["json", "stream"] }
async-trait = "0.1"
thiserror = "2"

[dev-dependencies]
decipher-mock-provider = { path = "../mock-provider" }
